# Продакшен API — прямой IP (POST не ломается редиректом Namecheap)
# runbonus.online → 302 → GET вместо POST → Cannot GET /api/auth/login
VITE_API_URL=http://161.129.67.147
# Продакшен API (домен сервера)
VITE_API_URL=https://runbonus.online
